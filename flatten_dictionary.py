def flatten_d(initial_dict, flatten_dict, delimiter="."):
    if initial_dict isinstance of dict:
        flatten_d(flatten_dict, key + delimiter = value)

    else:
        flatten_dict.append([key, value])